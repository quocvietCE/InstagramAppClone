import {StyleSheet} from 'react-native';

const styles = StyleSheet.create({
  container: {
    // height: 76,
    // width: 76,
    borderRadius: 40,
    borderWidth: 3,
    borderColor: '#ae22e0',
    margin: 8,
  },
  image: {
    // height: 70,
    // width: 70,
    borderRadius: 40,
    borderWidth: 3,
    borderColor: '#fff',
  },
});

export default styles;
